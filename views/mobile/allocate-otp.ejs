<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Drop OTP</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
html, body {
  height: 100%;
}

body {
  background: linear-gradient(180deg, #f8f9fb 0%, #eef1f6 100%);
  font-family: system-ui, -apple-system, BlinkMacSystemFont;
  margin: 0;
}

/* Navbar spacing */
.app-navbar-space {
  height: 60px;
}

/* Page container */
.page {
  width: 100%;
  max-width: 420px;
  margin: 0 auto;
  padding: 16px;
}


    /* Header */
    .page-header {
      text-align: center;
      margin-bottom: 20px;
    }

    .page-header h4 {
      font-weight: 700;
      margin-bottom: 4px;
    }

    .page-header p {
      font-size: 14px;
      color: #6c757d;
    }

    /* OTP Card */
    .otp-card {
      background: #ffffff;
      border-radius: 20px;
      padding: 24px;
      text-align: center;
      box-shadow: 0 12px 30px rgba(0,0,0,0.08);
      margin-bottom: 16px;
    }

    .otp-label {
      font-size: 13px;
      color: #6c757d;
      margin-bottom: 10px;
    }

    .otp-code {
      font-size: 2.6rem;
      font-weight: 800;
      letter-spacing: 14px;
      color: #198754;
      padding: 16px 12px;
      border-radius: 14px;
      background: #f3fff8;
      border: 2px dashed #198754;
    }

    /* Info Card */
    .info-card {
      background: #ffffff;
      border-radius: 16px;
      padding: 16px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.06);
      margin-bottom: 14px;
    }

    .info-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px 0;
      font-size: 14px;
    }

    .info-row span {
      color: #6c757d;
    }

    .info-row strong {
      font-weight: 600;
    }

    /* Warning */
    .warning-box {
      background: #fff7e6;
      border-radius: 14px;
      padding: 14px;
      font-size: 13px;
      color: #8a5a00;
      margin-bottom: 18px;
    }

    /* Buttons */
    .btn-lg-mobile {
      padding: 14px;
      font-size: 16px;
      border-radius: 14px;
    }
  </style>
</head>
<body>

<%- include('../partials/navbar') %>

<div class="app-navbar-space"></div>

<div class="page">

  <!-- Header -->
  <div class="page-header">
    <h4>Drop OTP Generated</h4>
    <p>Share this OTP with the gig worker</p>
  </div>

  <!-- OTP -->
  <div class="otp-card">
    <div class="otp-label">ONE-TIME PASSWORD</div>
    <div class="otp-code">
      <%= otp %>
    </div>
  </div>

  <!-- Info -->
  <div class="info-card">
    <div class="info-row">
      <span>Locker ID</span>
      <strong><%= lockerId %></strong>
    </div>

    <div class="info-row">
      <span>Compartment</span>
      <strong><%= compartmentId %></strong>
    </div>

    <div class="info-row">
      <span>Valid FOR</span>
      <strong>
10 Minutes
      </strong>
    </div>
  </div>

  <!-- Warning -->
  <div class="warning-box">
    ⚠️ This OTP is valid for <strong>one-time use only</strong>.  
    Share it only with the courier person.
  </div>

  <!-- Actions -->
  <div class="d-grid gap-2">
    <a href="/mobile/allocate" class="btn btn-success btn-lg-mobile">
      Allocate Another Locker
    </a>

    <a href="/" class="btn btn-outline-secondary btn-lg-mobile">
      Go to Home
    </a>
  </div>

</div>

</body>

</html>
