<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Drop OTP</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      background: #f8f9fa;
    }
    .otp-box {
      letter-spacing: 10px;
      font-size: 2.5rem;
      font-weight: 700;
      text-align: center;
      background: #ffffff;
      border-radius: 12px;
      padding: 20px;
      border: 2px dashed #198754;
    }
    .info-card {
      background: #ffffff;
      border-radius: 12px;
      padding: 16px;
    }
  </style>
</head>
<body>

<div class="container py-4">

  <!-- Header -->
  <div class="text-center mb-4">
    <h4 class="fw-bold">Drop OTP Generated</h4>
    <p class="text-muted mb-0">
      Share this OTP with the gig worker
    </p>
  </div>

  <!-- OTP Display -->
  <div class="otp-box mb-3">
    <%= otp %>
  </div>

  <!-- Info -->
  <div class="info-card mb-3">
    <div class="d-flex justify-content-between mb-2">
      <span class="text-muted">Locker ID</span>
      <strong><%= lockerId %></strong>
    </div>

    <div class="d-flex justify-content-between mb-2">
      <span class="text-muted">Compartment</span>
      <strong><%= compartmentId %></strong>
    </div>

    <div class="d-flex justify-content-between">
      <span class="text-muted">Valid Till</span>
      <strong>
        <%= new Date(expiresAt).toLocaleTimeString("en-IN", {
          hour: "2-digit",
          minute: "2-digit"
        }) %>
      </strong>
    </div>
  </div>

  <!-- Warning -->
  <div class="alert alert-warning small">
    ⚠️ This OTP is valid for one-time use only.  
    Do not share it with anyone except the assigned gig worker.
  </div>

  <!-- Actions -->
  <div class="d-grid gap-2">
    <a href="/mobile/allocate" class="btn btn-primary">
      Allocate Another Locker
    </a>

    <a href="/" class="btn btn-outline-secondary">
      Go to Home
    </a>
  </div>

</div>

</body>
</html>
