<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex align-items-center justify-content-center" style="height:100vh;">
  <div class="card shadow-lg p-4" style="width: 420px; border-radius: 16px;">
    <h4 class="text-center mb-3"><%= title %></h4>
    <p class="text-center text-muted">Reference ID: <strong><%= ref %></strong></p>
    
    <div class="alert alert-info mt-3 text-center">
      <%= message %>
    </div>

    <% if (results && results.length > 0) { %>
      <ul class="list-group mt-3">
        <% results.forEach(r => { %>
          <li class="list-group-item">
            <strong>Extension:</strong> <%= r?._id || 'N/A' %><br>
            <small>Status: <%= r?.status || 'Unknown' %></small>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="text-center text-muted mt-3">No pending extensions found.</p>
    <% } %>

    <div class="text-center mt-4">
      <a href="/" class="btn btn-primary">Go to Dashboard</a>
    </div>
  </div>
</body>
</html>
