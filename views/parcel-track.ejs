<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Shipment | LogisticsApp</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #4f46e5;
            --bg: #f3f4f6;
            --text: #1f2937;
            --text-light: #6b7280;
            --white: #ffffff;
            --radius: 16px;
        }

        body {
            background-color: var(--bg);
            font-family: 'Inter', sans-serif;
            color: var(--text);
            margin: 0;
            padding: 40px 16px;
        }

        .tracking-container {
            max-width: 600px;
            margin: 0 auto;
        }

        /* Top Summary Card */
        .status-card {
            background: var(--white);
            border-radius: var(--radius);
            padding: 24px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            margin-bottom: 24px;
            border: 1px solid #e5e7eb;
        }

        .status-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }

        .status-badge {
            background: #e0e7ff;
            color: var(--primary);
            padding: 6px 12px;
            border-radius: 99px;
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .meta-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
            border-top: 1px solid #f3f4f6;
            padding-top: 20px;
        }

        .meta-item label {
            display: block;
            font-size: 0.75rem;
            color: var(--text-light);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 4px;
        }

        .meta-item span {
            font-weight: 600;
            font-size: 0.95rem;
        }

        /* Timeline / Stepper CSS */
        .timeline-card {
            background: var(--white);
            border-radius: var(--radius);
            padding: 32px 24px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .timeline {
            position: relative;
            padding-left: 32px;
            list-style: none;
        }

        /* The vertical line */
        .timeline::before {
            content: '';
            position: absolute;
            left: 7px;
            top: 5px;
            bottom: 5px;
            width: 2px;
            background: #e5e7eb;
        }

        .timeline-item {
            position: relative;
            padding-bottom: 32px;
        }

        .timeline-item:last-child {
            padding-bottom: 0;
        }

        /* The Dot */
        .timeline-dot {
            position: absolute;
            left: -32px;
            width: 16px;
            height: 16px;
            background: var(--white);
            border: 3px solid #d1d5db;
            border-radius: 50%;
            z-index: 2;
        }

        /* Highlight the most recent activity */
        .timeline-item:first-child .timeline-dot {
            border-color: var(--primary);
            background: var(--primary);
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.2);
        }

        .activity-title {
            font-weight: 600;
            font-size: 1rem;
            display: block;
            margin-bottom: 4px;
        }

        .activity-meta {
            font-size: 0.85rem;
            color: var(--text-light);
            display: flex;
            gap: 12px;
        }

        .activity-location {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .empty-state {
            text-align: center;
            padding: 40px 0;
            color: var(--text-light);
        }

        @media (max-width: 480px) {
            .meta-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

<div class="tracking-container">
    <div class="status-card">
        <div class="status-header">
            <div>
                <h4 style="margin: 0; font-size: 1.25rem;">📦 Tracking Details</h4>
                <p style="margin: 4px 0 0; color: var(--text-light); font-size: 0.9rem;">AWB: <%= parcel.transitInfo.awb %></p>
            </div>
            <span class="status-badge"><%= tracking.current_status %></span>
        </div>

        <div class="meta-grid">
            <div class="meta-item">
                <label>Courier</label>
                <span><%= tracking.courier_name %></span>
            </div>
            <div class="meta-item">
                <label>Access Code</label>
                <span><%= parcel.accessCode %></span>
            </div>
        </div>
    </div>

    <div class="timeline-card">
        <h5 style="margin: 0 0 24px 0; font-size: 1.1rem;">📍 Shipment Timeline</h5>
        
        <% if (tracking.shipment_track_activities?.length) { %>
            <div class="timeline">
                <% tracking.shipment_track_activities.forEach((step, index) => { %>
                    <div class="timeline-item">
                        <div class="timeline-dot"></div>
                        <span class="activity-title"><%= step.activity %></span>
                        <div class="activity-meta">
                            <span><%= step.date %></span>
                            <span class="activity-location">
                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
                                <%= step.location %>
                            </span>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="empty-state">
                <p>No tracking updates available yet.</p>
                <small>Updates usually appear 12-24 hours after pickup.</small>
            </div>
        <% } %>
    </div>
</div>

</body>
</html>