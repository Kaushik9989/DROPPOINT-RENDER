<!DOCTYPE html>
<html>
<head>
  <title>Create Parcel</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-body">
      <h4 class="mb-3">📦 Create Courier Parcel</h4>

      <form method="POST" action="/api/parcel/create">

        <h6>Sender</h6>
        <input name="senderName" class="form-control mb-2" placeholder="Your Name" required>
        <input name="senderPhone" class="form-control mb-3" placeholder="Your Phone" required>

        <h6>Receiver</h6>
        <input name="receiverName" class="form-control mb-2" placeholder="Receiver Name" required>
        <input name="receiverPhone" class="form-control mb-3" placeholder="Receiver Phone" required>

        <h6>Delivery Address</h6>
        <textarea name="delivery_address" class="form-control mb-2" placeholder="Full Address" required></textarea>
        <input name="delivery_city" class="form-control mb-2" placeholder="City" required>
        <input name="delivery_state" class="form-control mb-2" placeholder="State" required>
        <input name="delivery_pincode" class="form-control mb-3" placeholder="Pincode" required>

        <h6>Parcel Size</h6>
        <select name="size" class="form-select mb-4" required>
          <option value="">Select Size</option>
          <option value="small">Small</option>
          <option value="medium">Medium</option>
          <option value="large">Large</option>
        </select>

        <button class="btn btn-primary w-100">
          Create Parcel
        </button>

      </form>
    </div>
  </div>
</div>

</body>
</html>
