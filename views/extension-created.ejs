<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Slightly larger touch targets & comfortable spacing for mobile */
    body { -webkit-font-smoothing:antialiased; }
    .card {
      border-radius: 14px;
      box-shadow: 0 6px 20px rgba(15, 23, 42, 0.06);
    }
    .meta { font-size: 0.95rem; color: #6b7280; }

    /* Buttons full-width on small screens, auto on md+ */
    .btn-responsive { width: 100%; }
    @media (min-width: 768px) {
      .btn-responsive { width: auto; min-width: 160px; }
    }

    /* Reduce card horizontal padding on tiny screens */
    .card-body { padding: 1.25rem; }
    @media (min-width: 768px) {
      .card-body { padding: 1.75rem; }
    }

    .small-note { font-size: 0.85rem; color: #6b7280; }
  </style>
</head>
<body class="bg-light">
  <div class="container py-4">
    <div class="mx-auto" style="max-width:880px;">
      <div class="card">
        <div class="card-body">
          <div class="d-flex align-items-start justify-content-between flex-column flex-md-row gap-3">
            <div>
              <h4 class="mb-1"><%= title %></h4>
              <div class="meta">Reference: <strong><%= ext.referenceId %></strong></div>
            </div>

            <div class="text-md-end small-note">
              <div>Amount: <strong>₹<%= ext.amount %></strong></div>
              <div class="mt-1">Hours: <strong><%= ext.hours %>h</strong></div>
            </div>
          </div>

          <hr class="my-3">

          <div class="row gy-3">
            <div class="col-12 col-md-6">
              <h6 class="mb-2">Parcel</h6>
              <p class="mb-1"><strong>ID:</strong> <%= parcel.customId || parcel._id %></p>
              <p class="mb-1"><strong>Access Code:</strong> <%= parcel.accessCode || '—' %></p>
              <p class="mb-1"><strong>Size:</strong> <%= parcel.size %></p>
            </div>

            <div class="col-12 col-md-6">
              <h6 class="mb-2">Extension</h6>
              <p class="mb-1"><strong>Rate/hr:</strong> ₹<%= ext.ratePerHour %></p>
              <p class="mb-1"><strong>Status:</strong> <%= ext.status %></p>
              <p class="mb-1"><small class="text-muted">Created: <%= ext.createdAt ? ext.createdAt.toLocaleString('en-IN', { timeZone: 'Asia/Kolkata' }) : '' %></small></p>
            </div>
          </div>

          <hr class="my-3">

          <div class="mb-3">
            <h6 class="mb-2">Payment Link</h6>
            <% if (link && link.short_url) { %>
              <p class="mb-2 small-note">Short URL:</p>
              <a class="d-inline-block text-truncate" style="max-width:100%;" href="<%= link.short_url %>" target="_blank" rel="noopener noreferrer"><%= link.short_url %></a>
              <p class="mb-0 mt-2 small-note"><small>Link ID: <%= link.id %></small></p>
            <% } else { %>
              <p class="text-muted">No payment link created.</p>
            <% } %>
          </div>

          <div class="mb-3">
            <h6 class="mb-2">WhatsApp</h6>
            <% if (whatsappResult) { %>
              <% if (whatsappResult.success) { %>
                <div class="alert alert-success py-2 mb-0">WhatsApp sent </div>
              <% } else { %>
                <div class="alert alert-warning py-2 mb-0">WhatsApp failed — <%= whatsappResult.info %></div>
              <% } %>
            <% } %>
          </div>

          <div class="d-grid gap-2 gap-md-0 mt-4 d-md-flex justify-content-between">
            <a href="/" class="btn btn-outline-secondary btn-lg btn-responsive">Dashboard</a>

            <% if (link && link.short_url) { %>
              <a href="<%= link.short_url %>" target="_blank" class="btn btn-success btn-lg btn-responsive">Open Payment Link</a>
            <% } %>
          </div>

          <p class="small-note mt-3 mb-0"><%= message %></p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
